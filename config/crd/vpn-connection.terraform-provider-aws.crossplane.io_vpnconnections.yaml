
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
  creationTimestamp: null
  name: vpnconnections.vpn-connection.terraform-provider-aws.crossplane.io
spec:
  group: vpn-connection.terraform-provider-aws.crossplane.io
  names:
    kind: VpnConnection
    listKind: VpnConnectionList
    plural: vpnconnections
    singular: vpnconnection
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VpnConnection is a managed resource representing a resource mirrored in the cloud
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: A VpnConnectionSpec defines the desired state of a VpnConnection
            properties:
              deletionPolicy:
                description: DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. The "Delete" policy is the default when no policy is specified.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: A VpnConnectionParameters defines the desired state of a VpnConnection
                properties:
                  customer_gateway_id:
                    type: string
                  id:
                    type: string
                  static_routes_only:
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    type: object
                  transit_gateway_id:
                    type: string
                  tunnel1_inside_cidr:
                    type: string
                  tunnel1_preshared_key:
                    type: string
                  tunnel2_inside_cidr:
                    type: string
                  tunnel2_preshared_key:
                    type: string
                  type:
                    type: string
                  vpn_gateway_id:
                    type: string
                type: object
              providerConfigRef:
                description: ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be used to create, observe, update, and delete this managed resource. Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            type: object
          status:
            description: A VpnConnectionStatus defines the observed state of a VpnConnection
            properties:
              atProvider:
                description: A VpnConnectionObservation records the observed state of a VpnConnection
                properties:
                  arn:
                    type: string
                  customer_gateway_configuration:
                    type: string
                  routes:
                    items:
                      properties:
                        destination_cidr_block:
                          type: string
                        source:
                          type: string
                        state:
                          type: string
                      type: object
                    type: array
                  transit_gateway_attachment_id:
                    type: string
                  tunnel1_address:
                    type: string
                  tunnel1_bgp_asn:
                    type: string
                  tunnel1_bgp_holdtime:
                    format: int64
                    type: integer
                  tunnel1_cgw_inside_address:
                    type: string
                  tunnel1_vgw_inside_address:
                    type: string
                  tunnel2_address:
                    type: string
                  tunnel2_bgp_asn:
                    type: string
                  tunnel2_bgp_holdtime:
                    format: int64
                    type: integer
                  tunnel2_cgw_inside_address:
                    type: string
                  tunnel2_vgw_inside_address:
                    type: string
                  vgw_telemetry:
                    items:
                      properties:
                        accepted_route_count:
                          format: int64
                          type: integer
                        last_status_change:
                          type: string
                        outside_ip_address:
                          type: string
                        status:
                          type: string
                        status_message:
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True, False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
